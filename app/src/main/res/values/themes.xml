<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ArbitrageTracker" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/theme_offwhite</item>
        <item name="colorPrimaryVariant">#786546</item>
        <item name="colorOnPrimary">@color/theme_textblack</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/theme_red</item>
        <item name="colorSecondaryVariant">@color/theme_green</item>
        <item name="colorOnSecondary">@color/theme_offwhite</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.ArbitrageTracker.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.ArbitrageTracker.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.ArbitrageTracker.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>